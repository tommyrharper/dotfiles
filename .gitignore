# herdr runtime artifacts (regenerated at runtime; never commit)
home/.config/herdr/*.log
home/.config/herdr/session.json
home/.config/herdr/*.sock
home/.config/herdr/plugins.json
home/.config/herdr/.plugins.lock
home/.config/herdr/release-notes.json

# macOS + nix build noise
.DS_Store
result
result-*

# Pi credentials and Home Manager adoption backups stay local
/home/.pi/agent/auth.json
/home/.pi/agent/**/*.backup

# Pi Calm is a runtime toggle; its local state file is never tracked
/home/.pi/agent/calm

# herdr's pi integration hook is regenerated by home.activation on every
# rebuild (~/.pi/agent/extensions is a tracked, symlinked directory) - never commit it
/home/.pi/agent/extensions/herdr-agent-state.ts

# Private shell values read by home.nix; copy from the example and keep local
/home/.config/zsh/private-env.zsh

# Private per-host SSH entries, Included into ~/.ssh/config by home.nix's
# activation script; copy from the .example and keep local
/home/.ssh/dotfiles.config.private

# no-mistakes local validation state - never commit in this public repo
.no-mistakes/
